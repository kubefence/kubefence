name: lint

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: gofmt
        run: |
          files=$(gofmt -l .)
          if [ -n "$files" ]; then
            echo "The following files are not gofmt'd:"
            echo "$files"
            echo ""
            echo "Run: gofmt -w ."
            exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: go mod tidy
        run: |
          go mod tidy
          if ! git diff --quiet go.mod go.sum; then
            echo "go.mod or go.sum is out of sync. Run: go mod tidy"
            git diff go.mod go.sum
            exit 1
          fi

      - name: go test
        run: go test ./... -count=1 -race

      # The Dockerfile pins its own golang tag, so setup-go's go-version-file
      # cannot catch it drifting behind go.mod. Only the builder stage runs —
      # the final stage needs the fetched nono binary, which release.yaml does.
      - name: docker build (builder stage)
        run: docker build --target builder .
