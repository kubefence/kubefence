apiVersion: v1
kind: Pod
metadata:
  name: nono-test
  namespace: default
  annotations:
    nono.sh/profile: "default"
spec:
  runtimeClassName: nono-sandbox
  restartPolicy: Never
  containers:
    - name: test
      image: alpine:3.20
      command: ["sleep", "infinity"]
