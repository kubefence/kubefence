{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nono-nri.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "nono-nri.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
