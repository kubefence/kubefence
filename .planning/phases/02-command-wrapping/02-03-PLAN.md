---
phase: 02-command-wrapping
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - internal/nri/integration_test.go
autonomous: true
requirements:
  - WRAP-01
  - WRAP-02
  - WRAP-03

must_haves:
  truths:
    - "Full injection flow works end-to-end: matching container gets correct args, mount, state; non-matching is untouched"
    - "State lifecycle is correct: created at CreateContainer, removed at RemoveContainer, pod dir cleaned when empty"
    - "All Phase 1 + Phase 2 tests pass in a single go test ./... invocation"
    - "No race conditions: go test -race passes"
  artifacts:
    - path: "internal/nri/integration_test.go"
      provides: "End-to-end Phase 2 integration tests"
      contains: "End-to-end injection"
  key_links:
    - from: "internal/nri/integration_test.go"
      to: "internal/nri/plugin.go"
      via: "Plugin.CreateContainer and Plugin.RemoveContainer"
      pattern: "p\\.CreateContainer|p\\.RemoveContainer"
---

<objective>
Add an end-to-end integration test that exercises the full Phase 2 injection lifecycle: CreateContainer for a matching container produces correct args, mount, and state dir; RemoveContainer cleans up the state dir; non-matching containers are unaffected. Run the full test suite with race detector as the Phase 2 gate.

Purpose: Final verification that all Phase 2 components work together correctly before marking the phase complete.
Output: Extended integration_test.go + full suite gate pass.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-command-wrapping/02-CONTEXT.md
@.planning/phases/02-command-wrapping/02-01-SUMMARY.md
@.planning/phases/02-command-wrapping/02-02-SUMMARY.md

@internal/nri/plugin.go
@internal/nri/integration_test.go
@internal/nri/adjustments.go
@internal/nri/state.go

<interfaces>
From internal/nri/adjustments.go:
```go
const ContainerNonoPath = "/nono/nono"
func BuildAdjustment(ctr *api.Container, profile, hostBinPath string) *api.ContainerAdjustment
```

From internal/nri/state.go:
```go
const StateBaseDir = "/var/run/nono-nri"
func WriteMetadata(podUID, containerID, pod, namespace, profile string) error
func RemoveMetadata(podUID, containerID string) error
func SetStateBaseDir(dir string)
func ResetStateBaseDir()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add end-to-end injection lifecycle integration test + full suite gate</name>
  <files>internal/nri/integration_test.go</files>
  <read_first>
    - internal/nri/integration_test.go (current state after Plan 02 updates — the file to extend)
    - internal/nri/plugin.go (CreateContainer/RemoveContainer — the code under test)
    - internal/nri/adjustments.go (ContainerNonoPath, BuildAdjustment — for assertions)
    - internal/nri/state.go (SetStateBaseDir, ResetStateBaseDir, StateBaseDir — for test isolation)
    - internal/nri/config.go (Config struct fields needed for test setup)
  </read_first>
  <action>
    Add a new Describe block to integration_test.go: `Describe("End-to-end injection lifecycle", func() { ... })`.

    **BeforeEach:** Create temp dir via `os.MkdirTemp("", "nono-e2e-*")`, call `nri.SetStateBaseDir(tmpDir)`. AfterEach: `nri.ResetStateBaseDir()`, `os.RemoveAll(tmpDir)`.

    **Test case 1: "full lifecycle for a sandboxed container"**
    - Config: `RuntimeClasses: []string{"nono-runc"}, DefaultProfile: "default", NonoBinPath: "/host/bin/nono"`
    - Pod: `RuntimeHandler: "nono-runc", Namespace: "prod", Name: "app-pod", Uid: "pod-uid-e2e", Annotations: map[string]string{"nono.sh/profile": "strict"}`
    - Container: `Id: "ctr-e2e-1", Args: []string{"python", "app.py", "--port", "8080"}`
    - Call CreateContainer:
      - Assert adj is not nil
      - Assert adj.Args equals `["/nono/nono", "wrap", "--profile", "strict", "--", "python", "app.py", "--port", "8080"]`
      - Assert adj.Mounts has len 1
      - Assert adj.Mounts[0].Source == "/host/bin/nono"
      - Assert adj.Mounts[0].Destination == "/nono/nono"
      - Assert adj.Mounts[0].Type == "bind"
      - Assert adj.Mounts[0].Options contains "bind", "ro", "rprivate"
    - Verify state dir: `os.Stat(filepath.Join(tmpDir, "pod-uid-e2e", "ctr-e2e-1", "metadata.json"))` succeeds
    - Read and unmarshal metadata.json: verify container_id, pod, namespace, profile, timestamp fields
    - Verify log output contains "injected" and all CORE-04 fields
    - Call RemoveContainer for same pod/container
    - Verify state dir removed: `os.Stat(filepath.Join(tmpDir, "pod-uid-e2e", "ctr-e2e-1"))` returns os.ErrNotExist
    - Verify pod dir removed (was empty): `os.Stat(filepath.Join(tmpDir, "pod-uid-e2e"))` returns os.ErrNotExist

    **Test case 2: "non-sandboxed container is completely untouched"**
    - Same Config but pod has `RuntimeHandler: "runc"` (not in RuntimeClasses)
    - Call CreateContainer: adj is nil, updates is nil, err is nil
    - Verify NO state dir created: `os.Stat(filepath.Join(tmpDir, podUID))` returns os.ErrNotExist
    - Verify log output contains "skip"

    **Test case 3: "multiple containers across pods with mixed injection"**
    - Two pods: one matching (nono-runc), one non-matching (runc). Each has a container.
    - CreateContainer for matching: adj non-nil, state written
    - CreateContainer for non-matching: adj nil, no state
    - RemoveContainer for matching: state cleaned
    - RemoveContainer for non-matching: no error (RemoveMetadata is safe on non-existent paths)

    Use `git commit -s` with commit message format per CLAUDE.md. Include `Assisted-by: AI` footer.

    **After committing, run the full suite gate:**
    ```bash
    go test ./... -v -count=1 -race
    go build ./...
    go vet ./...
    ```
    All three must pass. This is the Phase 2 completion gate.
  </action>
  <verify>
    <automated>cd /home/ubuntu/k8s-nono && go test ./... -v -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'End-to-end injection lifecycle' internal/nri/integration_test.go` matches
    - `grep 'full lifecycle for a sandboxed container' internal/nri/integration_test.go` matches
    - `grep 'non-sandboxed container is completely untouched' internal/nri/integration_test.go` matches
    - `grep 'metadata.json' internal/nri/integration_test.go` matches (state file verification)
    - `grep 'os.ErrNotExist' internal/nri/integration_test.go` matches (cleanup verification)
    - `go test ./... -v -count=1 -race` passes with 0 failures and no race conditions
    - `go build ./...` succeeds
    - `go vet ./...` succeeds
  </acceptance_criteria>
  <done>End-to-end integration tests verify the full Phase 2 injection lifecycle. Full test suite passes with race detector. Phase 2 gate is green.</done>
</task>

</tasks>

<verification>
Phase 2 completion gate:
- `go test ./... -v -count=1 -race` — all tests pass, no races
- `go build ./...` — clean build
- `go vet ./...` — no warnings
- New files exist: adjustments.go, adjustments_test.go, state.go, state_test.go
- Modified files: plugin.go, plugin_test.go, integration_test.go, main.go
</verification>

<success_criteria>
- Full injection lifecycle tested end-to-end: CreateContainer produces correct adjustment + state, RemoveContainer cleans up
- Non-sandboxed containers verified untouched (nil adj, no state)
- Race detector passes
- All Phase 1 + Phase 2 tests pass in one `go test ./...` invocation
- Build and vet clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-command-wrapping/02-03-SUMMARY.md`
</output>
