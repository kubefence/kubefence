---
phase: 02-command-wrapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/nri/adjustments.go
  - internal/nri/adjustments_test.go
  - internal/nri/state.go
  - internal/nri/state_test.go
autonomous: true
requirements:
  - WRAP-01
  - WRAP-02
  - WRAP-03

must_haves:
  truths:
    - "BuildAdjustment prepends /nono/nono wrap --profile <profile> -- before the container's existing args"
    - "BuildAdjustment adds a readonly bind mount from hostBinPath to /nono/nono inside the container"
    - "BuildAdjustment handles nil and empty container args without panic"
    - "WriteMetadata creates /var/run/nono-nri/{podUID}/{containerID}/metadata.json with correct JSON fields"
    - "RemoveMetadata removes the container state dir and best-effort removes the empty pod dir"
    - "RemoveMetadata is safe (no error) on non-existent paths"
  artifacts:
    - path: "internal/nri/adjustments.go"
      provides: "BuildAdjustment function and ContainerNonoPath constant"
      exports: ["BuildAdjustment", "ContainerNonoPath"]
    - path: "internal/nri/state.go"
      provides: "WriteMetadata, RemoveMetadata, StateBaseDir, ContainerMetadata"
      exports: ["WriteMetadata", "RemoveMetadata", "StateBaseDir", "ContainerMetadata"]
    - path: "internal/nri/adjustments_test.go"
      provides: "Unit tests for BuildAdjustment"
    - path: "internal/nri/state_test.go"
      provides: "Unit tests for WriteMetadata and RemoveMetadata"
  key_links:
    - from: "internal/nri/adjustments.go"
      to: "github.com/containerd/nri/pkg/api"
      via: "ContainerAdjustment.SetArgs and AddMount"
      pattern: "adj\\.SetArgs|adj\\.AddMount"
    - from: "internal/nri/state.go"
      to: "os"
      via: "MkdirAll, WriteFile, RemoveAll, Remove"
      pattern: "os\\.(MkdirAll|WriteFile|RemoveAll|Remove)"
---

<objective>
Create the two new internal packages that implement command wrapping and state management for Phase 2: `adjustments.go` (builds ContainerAdjustment with SetArgs + AddMount) and `state.go` (manages per-container metadata dirs). Both files are pure functions that can be fully unit tested without a live NRI runtime.

Purpose: These are the core building blocks that Plan 02 will wire into plugin.go. Separating them allows isolated, high-quality unit testing.
Output: Four new files — two implementation, two test.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-command-wrapping/02-CONTEXT.md
@.planning/phases/02-command-wrapping/02-RESEARCH.md

@internal/nri/plugin.go
@internal/nri/config.go
@internal/nri/plugin_test.go
@internal/nri/nri_suite_test.go
@internal/nri/filter_test.go

<interfaces>
<!-- Key types and contracts from the existing codebase and NRI API. -->

From internal/nri/config.go:
```go
type Config struct {
    RuntimeClasses []string `toml:"runtime_classes"`
    DefaultProfile string   `toml:"default_profile"`
    NonoBinPath    string   `toml:"nono_bin_path"`
    SocketPath     string   `toml:"socket_path"`
}
```

From github.com/containerd/nri/pkg/api (verified from module cache):
```go
// Container.GetArgs returns the existing OCI process args (nil if unset)
func (x *Container) GetArgs() []string

// ContainerAdjustment methods:
func (a *ContainerAdjustment) SetArgs(args []string)    // clones via slices.Clone
func (a *ContainerAdjustment) AddMount(m *Mount)        // appends to a.Mounts

// Mount struct (no Readonly bool — readonly via Options):
type Mount struct {
    Destination string
    Type        string
    Source      string
    Options     []string
}
```

From internal/nri/plugin_test.go (test package pattern):
```go
package nri_test  // external test package

import nri "github.com/k8s-nono/nono-nri/internal/nri"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create adjustments.go + adjustments_test.go (WRAP-01, WRAP-02)</name>
  <files>internal/nri/adjustments.go, internal/nri/adjustments_test.go</files>
  <read_first>
    - internal/nri/plugin.go (see how CreateContainer uses ctr *api.Container)
    - internal/nri/config.go (Config.NonoBinPath field)
    - internal/nri/plugin_test.go (test patterns: nri_test package, Ginkgo Describe/It, api struct construction)
    - internal/nri/filter_test.go (DescribeTable pattern for table-driven tests)
    - internal/nri/nri_suite_test.go (existing suite runner — no new suite needed)
  </read_first>
  <behavior>
    - Test: BuildAdjustment with existing args ["myapp", "--port", "8080"] and profile "strict" produces adj.Args == ["/nono/nono", "wrap", "--profile", "strict", "--", "myapp", "--port", "8080"]
    - Test: BuildAdjustment with nil args and profile "default" produces adj.Args == ["/nono/nono", "wrap", "--profile", "default", "--"]
    - Test: BuildAdjustment with empty []string{} args and profile "permissive" produces adj.Args == ["/nono/nono", "wrap", "--profile", "permissive", "--"]
    - Test: BuildAdjustment adds exactly 1 mount with Source=hostBinPath, Destination="/nono/nono", Type="bind", Options containing "bind", "ro", "rprivate"
    - Test: ContainerNonoPath constant equals "/nono/nono"
  </behavior>
  <action>
    **adjustments.go** (package nri):
    - Define `const ContainerNonoPath = "/nono/nono"`
    - Function `BuildAdjustment(ctr *api.Container, profile, hostBinPath string) *api.ContainerAdjustment`:
      1. Build prefix slice: `[]string{ContainerNonoPath, "wrap", "--profile", profile, "--"}`
      2. `newArgs := append(prefix, ctr.GetArgs()...)` — safe for nil/empty args
      3. `adj := &api.ContainerAdjustment{}`
      4. `adj.SetArgs(newArgs)`
      5. `adj.AddMount(&api.Mount{Source: hostBinPath, Destination: ContainerNonoPath, Type: "bind", Options: []string{"bind", "ro", "rprivate"}})`
      6. Return adj
    - Import only `api "github.com/containerd/nri/pkg/api"`

    **adjustments_test.go** (package nri_test):
    - Use Ginkgo DescribeTable for args prepend cases (3 entries: normal args, nil args, empty args)
    - Separate Describe block for mount verification
    - Assert on `adj.Args` field (set by SetArgs) and `adj.Mounts` slice (set by AddMount)
    - Import: `api "github.com/containerd/nri/pkg/api"`, `nri "github.com/k8s-nono/nono-nri/internal/nri"`, ginkgo/gomega

    Use `git commit -s` with commit message format per CLAUDE.md. Include `Assisted-by: AI` footer.
  </action>
  <verify>
    <automated>cd /home/ubuntu/k8s-nono && go test ./internal/nri/ -run TestNRI/BuildAdjustment -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func BuildAdjustment' internal/nri/adjustments.go` returns 1
    - `grep -c 'ContainerNonoPath' internal/nri/adjustments.go` returns at least 2 (const + usage)
    - `grep 'adj.SetArgs' internal/nri/adjustments.go` matches
    - `grep 'adj.AddMount' internal/nri/adjustments.go` matches
    - `grep '"bind", "ro", "rprivate"' internal/nri/adjustments.go` matches
    - `go test ./internal/nri/ -run TestNRI/BuildAdjustment -v -count=1` passes with 0 failures
    - Test file contains at least 3 test cases (normal args, nil args, empty args)
    - Test file verifies mount Source, Destination, Type, and Options fields
  </acceptance_criteria>
  <done>BuildAdjustment correctly prepends nono prefix to all arg variants and adds a readonly bind mount. All unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create state.go + state_test.go (WRAP-03)</name>
  <files>internal/nri/state.go, internal/nri/state_test.go</files>
  <read_first>
    - internal/nri/plugin.go (see CreateContainer/RemoveContainer signatures for args that state functions will receive)
    - internal/nri/plugin_test.go (test patterns: nri_test package, Ginkgo Describe/It)
    - internal/nri/nri_suite_test.go (existing suite runner)
    - .planning/phases/02-command-wrapping/02-RESEARCH.md (Pattern 3: state management)
  </read_first>
  <behavior>
    - Test: WriteMetadata("pod-uid-1", "ctr-1", "web", "prod", "strict") creates file at {baseDir}/pod-uid-1/ctr-1/metadata.json
    - Test: metadata.json contains valid JSON with fields container_id="ctr-1", pod="web", namespace="prod", profile="strict", and a non-empty timestamp in RFC3339 format
    - Test: RemoveMetadata("pod-uid-1", "ctr-1") removes the ctr-1 directory
    - Test: RemoveMetadata removes the pod-uid-1 parent dir if it is empty after container dir removal
    - Test: RemoveMetadata does NOT remove the pod dir if other container dirs still exist
    - Test: RemoveMetadata returns nil error for non-existent paths (no ENOENT error)
  </behavior>
  <action>
    **state.go** (package nri):
    - Define `const StateBaseDir = "/var/run/nono-nri"` and a package-level var `stateBaseDir = StateBaseDir` for test injection
    - Export test helpers: `func SetStateBaseDir(dir string)` and `func ResetStateBaseDir()` — follows the same setter pattern as kernel.go's SetKernelVersionFunc/ResetKernelVersionFunc
    - Define `type ContainerMetadata struct` with fields: `ContainerID string json:"container_id"`, `Pod string json:"pod"`, `Namespace string json:"namespace"`, `Profile string json:"profile"`, `Timestamp string json:"timestamp"`
    - Function `WriteMetadata(podUID, containerID, pod, namespace, profile string) error`:
      1. `dir := filepath.Join(stateBaseDir, podUID, containerID)`
      2. `os.MkdirAll(dir, 0700)` — return wrapped error on failure
      3. Construct `ContainerMetadata{ContainerID: containerID, Pod: pod, Namespace: namespace, Profile: profile, Timestamp: time.Now().UTC().Format(time.RFC3339)}`
      4. `json.Marshal(meta)` — return wrapped error on failure
      5. `os.WriteFile(filepath.Join(dir, "metadata.json"), data, 0600)` — return wrapped error on failure
    - Function `RemoveMetadata(podUID, containerID string) error`:
      1. `containerDir := filepath.Join(stateBaseDir, podUID, containerID)`
      2. `os.RemoveAll(containerDir)` — return wrapped error on failure
      3. `podDir := filepath.Join(stateBaseDir, podUID)`
      4. `_ = os.Remove(podDir)` — best-effort, ignore error (fails if non-empty)
      5. Return nil
    - Imports: `encoding/json`, `fmt`, `os`, `path/filepath`, `time`

    **state_test.go** (package nri_test):
    - Use `GinkgoT()` with `os.MkdirTemp` in BeforeEach to create temp base dir; call `nri.SetStateBaseDir(tmpDir)`
    - AfterEach: `nri.ResetStateBaseDir()` + `os.RemoveAll(tmpDir)`
    - Test WriteMetadata: verify dir exists, file exists, unmarshal JSON and check all 5 fields
    - Test RemoveMetadata: create metadata first, remove, verify `os.Stat` returns `os.ErrNotExist`
    - Test RemoveMetadata cleans pod dir: single container, remove it, pod dir should not exist
    - Test RemoveMetadata leaves pod dir: two containers, remove one, pod dir should still exist
    - Test RemoveMetadata on non-existent path: returns nil
    - Import: `encoding/json`, `os`, `path/filepath`, ginkgo/gomega, `nri "github.com/k8s-nono/nono-nri/internal/nri"`

    Use `git commit -s` with commit message format per CLAUDE.md. Include `Assisted-by: AI` footer.
  </action>
  <verify>
    <automated>cd /home/ubuntu/k8s-nono && go test ./internal/nri/ -run TestNRI/State -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func WriteMetadata' internal/nri/state.go` returns 1
    - `grep -c 'func RemoveMetadata' internal/nri/state.go` returns 1
    - `grep 'StateBaseDir' internal/nri/state.go` matches (exported constant)
    - `grep 'ContainerMetadata' internal/nri/state.go` matches (exported struct)
    - `grep 'SetStateBaseDir\|ResetStateBaseDir' internal/nri/state.go` matches both
    - `grep '0700' internal/nri/state.go` matches (dir permissions)
    - `grep '0600' internal/nri/state.go` matches (file permissions)
    - `go test ./internal/nri/ -run TestNRI/State -v -count=1` passes with 0 failures
    - Test file contains at least 5 test cases covering write, remove, empty parent cleanup, non-empty parent preservation, non-existent path
  </acceptance_criteria>
  <done>WriteMetadata creates proper state dirs with valid JSON metadata. RemoveMetadata cleans up correctly with best-effort parent removal. All unit tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `go test ./internal/nri/ -v -count=1` — all existing Phase 1 tests still pass alongside new Phase 2 tests
- `go vet ./internal/...` — no vet warnings
- `go build ./...` — builds cleanly
</verification>

<success_criteria>
- adjustments.go exports BuildAdjustment and ContainerNonoPath
- state.go exports WriteMetadata, RemoveMetadata, StateBaseDir, ContainerMetadata, SetStateBaseDir, ResetStateBaseDir
- All new unit tests pass: `go test ./internal/nri/ -run "TestNRI/(BuildAdjustment|State)" -v -count=1`
- All existing Phase 1 tests still pass: `go test ./internal/... -count=1`
- Zero new dependencies added to go.mod
</success_criteria>

<output>
After completion, create `.planning/phases/02-command-wrapping/02-01-SUMMARY.md`
</output>
